# Line-of-Code Budget

Status: active

Supersedes: `docs/budget-2026-07-06.md`

Ratified by: `docs/adr/0009-loc-budget-56k.md` (mechanism); this revision is a
flat +10% across-the-board increase approved by Eli 2026-07-07: the 56K-era
numbers were sized for the v0.1 release, and post-release feature work
(round observer, resume, CLI wiring) begins against v0.2.

Single change from 2026-07-06: **all allocations and the binding ceiling
+10%** (rounded to the nearest 5). No structural changes; all rules carry
over:

- `euler-cli` red-row discipline carries over at the new number: it was
  1,686 over the old allocation and is now under the new one, but the
  post-release TUI consolidation pass stays queued and every commit
  touching euler-cli still discloses the number.
- `euler-core` at 12,500 actual was exactly at the old allocation; the
  session.rs consolidation pass (2,282 lines) remains queued before v0.2.
- Extension crates are advisory-only (`euler-extension-session-export`,
  `euler-extension-causal-dag`); core boundary rules per the Boundary Test
  still apply.
- Budgets cover non-test Rust source as counted by `scripts/loc_report.sh`
  (excludes `tests/` dirs and `tests.rs`, `*_test.rs`, `*_tests.rs`).

Per-crate allocations are local guardrails, not additive permission to
exceed the binding 61.6K hard ceiling.

| Area | Crates | Local allocation |
|---|---|---:|
| CLI / terminal UI | `euler-cli` | 27,775 |
| Core session loop, canvas, dispatch, provenance | `euler-core` | 13,750 |
| Provider / model routing | `euler-provider` | 7,150 |
| Built-in tools and renderer lab | `euler-tools` | 6,050 |
| Event envelope | `euler-event` | 1,100 |
| Extension SDK | `euler-sdk` | 2,365 |
| Multi-agent scaffolding | `euler-agents` | 2,200 |
| **Binding core ceiling** | | **61,600** |
